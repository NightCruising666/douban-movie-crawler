# 旧数据归档

本目录中的 CSV 只用于保留历史采集成果，不参与当前断点计算、数据清洗或统计分析。

`legacy_before_schema_v3/` 包含：

- 旧版 499 部电影详情（无豆瓣ID主键）。
- 新旧 schema 切换前的 135 部部分详情。
- 旧版短评样本（归档时已将公开用户名转换为不可逆匿名标识）。
- 早期小规模试采文件。

当前代码只读取 `data/` 直接目录下的正式文件。
